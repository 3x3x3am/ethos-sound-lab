import SwiftUI

enum Theme {
    static let bg = Color.black
    static let panel = Color(red: 0.10, green: 0.10, blue: 0.12)
    static let panel2 = Color(red: 0.14, green: 0.14, blue: 0.18)
    static let text = Color.white
    static let accent = Color(hue: 0.77, saturation: 0.65, brightness: 0.95) // soft neon purple
}
